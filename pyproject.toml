[project]
name = "f4ncy-logs"
version = "0.1.9"
requires-python = ">=3.13"
dependencies = [
    "loguru",
    "rich",
    "rich-toolkit",
    "tzdata"
]

[dependency-groups]
dev = [
    "pyrefly>=0.45.0",
]

[build-system]
requires = ["uv_build>=0.11.12,<0.12"]
build-backend = "uv_build"
