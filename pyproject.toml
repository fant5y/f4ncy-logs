[project]
name = "f4ncy-logs"
version = "0.1.9"
requires-python = ">=3.12"
dependencies = [
    "loguru",
    "rich",
    "rich-toolkit",
    "tzdata"
]

[dependency-groups]
dev = [
    "pyrefly>=0.45.0",
]

[build-system]
requires = ["uv_build>=0.12.0,<=0.13.0"]
build-backend = "uv_build"
