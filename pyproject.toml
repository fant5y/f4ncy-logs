[project]
name = "f4ncy-logs"
version = "0.1.6"
requires-python = ">=3.13"
dependencies = [
    "loguru",
    "pyrefly>=0.45.0",
    "rich",
    "rich-toolkit",
]

[build-system]
requires = ["uv_build>=0.11.12,<0.12"]
build-backend = "uv_build"
